package com.enjplatform.reconciliation.before;

/**
 * @author starslink
 */
public class DefaultResourceReader extends AbstractResourceReader {

    public DefaultResourceReader(ResourceLoader sourceLoader, ResourceLoader targetLoader) {
        super(sourceLoader, targetLoader);
    }
}
